import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.brandingcopilot',
  appName: 'Branding-Co-pilot',
  webDir: 'dist'
};

export default config;
